import React from "react";

/**
 * PUBLIC_INTERFACE
 * ModalRoot can stack/inject modals centrally if needed (placeholder)
 */
function ModalRoot() {
  // For now, no global modals. Leave for expansion.
  return null;
}

export default ModalRoot;
